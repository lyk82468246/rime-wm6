//
// rime/config.h -- WinCE-port mirror of upstream config.h.
//
// Convenience umbrella for `<rime/config/config_component.h>` (which
// declares Config) and `<rime/config/config_types.h>` (which declares
// ConfigItem / ConfigValue / ConfigList / ConfigMap). Byte-identical to
// upstream; the trimming happens behind the umbrella in config_component.h.
//
#ifndef RIME_CONFIG_H_
#define RIME_CONFIG_H_

#include <rime/config/config_component.h>
#include <rime/config/config_types.h>

#endif  // RIME_CONFIG_H_
